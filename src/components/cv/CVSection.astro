---
interface Props {
  title: string;
  id?: string;
}

const { title, id } = Astro.props;
---

<a class="anchor" id={id}></a>
<section class="folio-row">
  <p class="folio-label" set:html={title}></p>
  <div class="folio-body">
    <slot />
  </div>
</section>
