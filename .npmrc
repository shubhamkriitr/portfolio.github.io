# eslint 10 conflicts with eslint-plugin-jsx-a11y's peer range (dev-only lint tooling,
# not used by the Astro build). Allow install to proceed for both `npm install` and `npm ci`.
legacy-peer-deps=true
